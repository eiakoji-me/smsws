server:
  port: 8082
spring:
  application:
    name: COURSE-SERVICE
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure.include=*
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always