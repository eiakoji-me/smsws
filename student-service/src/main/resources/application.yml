server:
  port: 8083
spring:
  application:
    name: STUDENT-SERVICE
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure.include=*
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
